
<div class="author">
    <h1 class = "author__heading">Ket qua</h1>
</div>

<section class="articles">
    <ul class="article-ul">
        <% post.forEach(post => { %>
            <li>
                <a href="/posts/<%= post.id %>">
                    <span><%= post.title %></span>
                    <span class="article-list__date"><%= post.createdAt.toDateString() %></span>
                </a>
            </li>
        <% }) %> 
    </ul>
    
</section>

